API_USER=
API_PASSWORD=

# Semver tag for this build, must match the GitHub release tag exactly (e.g. v1.0.0)
VERSION=v0.1.0-SNAPSHOT

# GitHub repo in "owner/repo" format — used for autoupdate checks
# Example: GITHUB_REPO=myuser/H3SaveWatcher
GITHUB_REPO=
